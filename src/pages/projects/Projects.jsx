import React from 'react';
import {
  PageWrapper, Title, ContentContainer,
} from './ProjectsStyles';

const AboutMe = () => (
  <PageWrapper>
    <ContentContainer>
      <Title>Projects</Title>
    </ContentContainer>
  </PageWrapper>
);

export default AboutMe;
